{
  "name": "human-evolution-skull-comparison",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve-public.mjs --host 127.0.0.1 --port 4173",
    "serve": "node scripts/serve-public.mjs --host 127.0.0.1 --port 4173",
    "build:images": "node scripts/build-images.mjs",
    "build:models": "node scripts/build-models.mjs",
    "build": "npm run build:images && npm run build:models",
    "test": "npm run test:security",
    "test:security": "node scripts/serve-public.test.mjs"
  },
  "devDependencies": {
    "@gltf-transform/cli": "^4.3.0",
    "@google/model-viewer": "^4.2.0",
    "playwright-core": "^1.60.0",
    "sharp": "^0.34.5"
  }
}
